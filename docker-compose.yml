version: "3"

services:
  app:
    networks:
      default:
        beaubox-home: beaubox-home_default
    build:
      restart: always
      context: ./
      dockerfile: Dockerfile
      args:
        USERID: 0
        GROUPID: 0
    ports:
      - '3000:3000'
